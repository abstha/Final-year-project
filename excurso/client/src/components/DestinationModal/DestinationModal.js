import {React,useState} from 'react'
import {
  Dialog,
  DialogTitle,
  DialogContent,
  DialogContentText,
  DialogActions,
  Button,
} from "@mui/material";


const DestinationModal = ({ destination, onClose, open }) => {
    
    return (
      <h3>asdas</h3>
    );
  };
  
export default DestinationModal